// Presale UI temporarily replaced by Coming Soon stub. The original component
// lives in git history; re-enable when the next round is configured.
export default function PresaleBar() {
  return null;
}
